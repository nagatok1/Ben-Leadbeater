7e. not a god

8.select movie_id, count(movie_id) as "number of reviews" from ratings group by movie_id order by count(movie_id) desc limit 5;

9.mysql> select movies.title, ratings.movie_id, ratings.rating, count(ratings.movie_id) as "number of reviews" from ratings left join movies on ratings.movie_id = movies.id group by ratings.movie_id having count(ratings.movie_id) > 10 order by ratings.rating desc limit 10;

10.mysql> select movies.title, ratings.movie_id, ratings.rating, count(ratings.movie_id) as "number of reviews" from ratings left join movies on ratings.movie_id = movies.id group by ratings.movie_id having count(ratings.movie_id) > 10 order by ratings.rating limit 10;







